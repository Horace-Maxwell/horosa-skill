"""每个接入面的**唯一问题构造点**（instructions 英文、选项键 ASCII、必带弃权项）。

面与权限（AGENTS §4 决策层法则）：
- `routing`  S1：确定性路由无解时兜底（两级 Choice：族 → 工具）。
- `extract`  S2：从原话**明说**的值里抽取（词表出候选、Jev 在候选里选、双钥才填）。
- `zhancat`  S3：六壬占断门类分类（闭集 + general）。
"""
